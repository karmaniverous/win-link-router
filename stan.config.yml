stan-core:
  stanPath: .stan
  excludes:
    - '**/.tsbuild/**'
    - '**/*.tsbuildinfo'
    - CHANGELOG.md
    - package-lock.json
stan-cli:
  cliDefaults:
    run:
      facets: false
      plan: false
      sequential: true
  patchOpenCommand: code -g {file}
  scripts:
    typecheck: npm run typecheck
    lint:
      script: npm run lint:fix
      warnPattern: \\d+:\\d+\\s+warning
    test: npm run test -- --no-color
    knip:
      script: npm run knip
      warnPattern: Configuration hints
    package: npx dotenvx run -e NO_COLOR=1 --ignore=MISSING_ENV_FILE -- npm run package
    make: npx dotenvx run -e NO_COLOR=1 --ignore=MISSING_ENV_FILE -- npm run make
